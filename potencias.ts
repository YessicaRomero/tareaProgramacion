import * as rls from "readline-sync";

let base: number = rls.questionInt("ingrese la base: ");
let exponente: number = rls.questionInt("ingrese el exponente: ");
let cantidadDeExpo: number =(base ** exponente);

function potencias(base:number, exponente:number){
return cantidadDeExpo        
    

 
} console.log(base, cantidadDeExpo)


